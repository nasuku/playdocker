A sample 2-node ubuntu vagrantfile.
example provision (in this case to install ipsec-tools) by 
    installing a package 
    copying files from host system to target image
    Run a command on target VM 
